apiVersion: apps/v1
kind: Deployment
metadata:
  name: test-deployment
spec:
  template:
    spec:
      containers:
      - name: test
        env:
          - name: PEAR
            value: "not an apple"
          - name: PLUM
            valueFrom:
              configMapKeyRef:
                name: fruitconf
                key: plumkey